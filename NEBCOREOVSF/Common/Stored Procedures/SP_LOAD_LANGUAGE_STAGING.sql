CREATE OR ALTER PROCEDURE  COMMON.SP_LOAD_LANGUAGE_STAGING()
RETURNS VARCHAR
LANGUAGE SQL
EXECUTE AS OWNER
AS 
'BEGIN
    COPY INTO Common.STAGING_LANGUAGE
    FROM @ONEVIEW_STAGE/admin/languages/
    MATCH_BY_COLUMN_NAME = CASE_INSENSITIVE
    FILE_FORMAT = (TYPE = ''PARQUET'')
    ON_ERROR=CONTINUE
    FORCE = TRUE;

    RETURN 1;

END';